- 여기까지 됨 (2026-09-24 열여덟째 — 구성표 클릭·움직임): 구성표 클릭(4배 감속 90–130ms)의 대부분은 스타일 다시 계산이다. `data-scheme` 을 바꾸면 url() 커서 규칙이 목록 칸 약 1,276개에 다시 걸려 40ms, 패널 켜고 끄기가 스타일 20ms + 배치 17ms 다. 세 가지를 재 봤다. 갈래·칸에 content-visibility 는 실제 스크롤 뒤 클릭에서 중앙값 110→128·120ms 로 이득이 없어 뺐다. 커서 규칙을 `:hover` 로 좁히는 안은 112→111ms 라 build.py 는 안 고쳤다. 커스텀 속성 커서도 효과가 없었다. 대신 진짜 버벅임을 고쳤다. 움직임을 50ms 마다 표본 떠서 33ms·16ms 프레임이 들쭉날쭉 넘어가던 것을, 다음 장이 바뀌는 때에 맞춘 setTimeout 으로 바꿨다. 전기는 33.3ms, 폭죽은 16.7ms 간격으로 넘어간다. 장 번호가 같으면 일을 안 만들고, 움직이는 그림은 `will-change` 로 제 레이어에 둔다. 제 속도로 도니 CPU 는 더 쓴다(전기·폭죽 정지 상태 37→약 60%). WebKitGTK 빈 장 0, test_mac 실패 0
- 다음 할 것: 아이폰·PC 에서 전기·폭죽 움직임이 매끄러워졌는지, 폰이 뜨거워지지 않는지 볼 것. 구성표 클릭을 더 줄이려면 커서 CSS 구조를 바꿔야 한다(build.py 수정 → 전체 빌드 두 번)
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열일곱째 — 맥 버튼 자리): "맥에서 쓰기" 칸에 따로 있던 **맥용 .cape 받기** 를 화면 맨 아래 줄 **적용** 자리로 옮겼다. 윈도우가 아니면 그 버튼이 "보기" 대신 맥용 .cape 받기(굽는 중… → 받음 · N KiB)이고, 맥에선 그 줄의 원래대로를 숨긴다(Mousecape ⌘R). 굽는 코드는 `window.cpCape()` 로 내놓고 `test_mac.js` 도 그것을 부른다(627벌 실패 0). 크로미움 맥 흉내 390·1440px 에서 가로 넘침 없고 누르면 받아짐, 윈도우 흉내는 그대로 적용
- 다음 할 것: 맥에서 Pages 판 오른쪽 아래 버튼으로 받아 Mousecape 에 넣어 볼 것. 붙여넣은 주소는 윈도우와 똑같이 첫 누름에 조합만 바꾸고 둘째 누름에 받는다
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열여섯째 — 재질 19종): 재질 카테고리를 재질이 읽히게 다시 그렸다. 녹은 얼룩, 벨벳은 가운데가 밝은 융단, 벽돌은 러닝 본드, 고무는 성긴 돌기로 바꿨다. 녹·강철은 이동 머리를 5칸으로, 십자·I빔을 2칸 굵기로 했고, 얼음은 성에 안개를 걷어 대비 테로 바꿨다. 매끈한 모양에서 번짐 층은 첫 테의 50% 이상이 차야 켜진다(`smooth._lit`). 상하좌우에만 테를 두르면 46–48%라 조용히 꺼지므로 대각선까지 두르되, 세 면이 막힌 좁은 틈은 비운다. 무늬와 몸 색이 채널당 115 이하로 차이 나면(`smooth.KEEP`) 둥근 모양에서 섞여 번진다. 코드는 안 고쳤다
- 다음 할 것: Pages 버전 도장이 이 merge 커밋인지 확인. 남은 결정 — 어두운 재질(벨벳·가죽·데님·고무)은 네온이 흐림(고치려면 smooth.py, 57종 다시 그림), 크기 20% 규칙을 테 빼고 불투명 몸으로 잴지, 호박 손이 다른 손보다 한 치수 작음
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열다섯째 — 시안 페이지 성능): 칸 하나의 프레임을 세로 스트립 PNG 한 장으로 잇고(프레임 뒤마다 판 1/8 투명 줄), 페이지는 그림 주소를 갈지 않고 `--f` 로 자리만 옮긴다. 커서 프레임은 스트립에서 캔버스로 잘라 낸다. 다른 모양은 `data/<모양>/index.json`(칸 정보만, gzip 1.5KB) + 구성표마다 한 파일 + `arrows.json`(목록 썸네일, gzip 2.4MB, 모양을 바꾼 뒤에 받음)으로 나눴고, 썸네일 잉크 상자는 빌드가 잰다. 크롬 CPU 4배 느리게: DCL 2319→1224ms, 모양 바꾸기 1.7초→0.17초. 아이폰 13 흉내(CPU 4배·8Mbps·gzip): DCL 4.1–5.3→1.8초, 둥근 모양 고름 0.24초·카드 0.4초·썸네일 3.9초. preview.html 6.0→2.0MB, data 218→98MB. WebKitGTK 빈 장 0. 컨테이너 전체 빌드 641–653초는 스트립 탓이 아니다 — 폭죽 × 둥근 일감이 전후 커밋 모두 45.7초, 그 사이 늘어난 그림(폭죽 144장 등) 몫
- 다음 할 것: Pages 에서 새 버전이 뜨는지, 아이폰 사파리에서 첫 화면·모양 바꾸기가 매끄러운지 사용자가 볼 것. 구성표 클릭은 여전히 90–130ms(4배 감속) — 다음 손잡이는 구성표 바꿀 때의 레이아웃
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열넷째 — 맥 버튼 검사): 다른 세션이 커서를 고쳐도 맥 버튼이 따라가는지 CI 가 보게 했다. `win-cursor/test_mac.js` 가 버튼 코드를 `preview.tpl.html` 에서 그대로 떼어 node 에서 dist 를 읽혀 57종 × 11모양 = 627벌을 굽고, 맥 이름 33개·장 수 24 이하·핫스팟·1x/2x 그림판 크기를 본다. 일부러 깨 본 넷(애니 구성표에 .cur 한 칸, .cur 항목 자리 어긋남, mac.json 에 없는 칸, 64px 판 빠짐) 중 앞의 셋은 걸리고, 64px 가 빠지면 버튼이 가장 큰 판을 줄여 쓰므로 통과가 맞다. 지금 빈틈 하나: 버튼은 구성표 단위로 확장자를 정해서(`animated()`), 움직이는 구성표에 가만한 칸이 하나 생기면 그 구성표는 맥에서 안 구워진다 — 지금은 그런 구성표가 없고, 생기면 이 검사가 잡는다
- 다음 할 것: main 에 들어갔다(윈도우 러너에서 6초). 이 스텝이 빨개지면 그 커밋이 맥 버튼을 깬 것 — 실패 줄이 까닭(없는 파일·장 수·핫스팟 등)을 적는다. 맥에서 Pages 판 `.cape` 를 한 번 적용해 보는 것은 아직
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열셋째 — 맥용 .cape 버튼): 사용자 맥에서 전기·폭죽 `.cape` 가 봐줄 만하다고 해서 시안 페이지에 "맥에서 쓰기" 칸과 **맥용 .cape 받기** 버튼을 달았다. 지금 고른 구성표·모양·색조를 브라우저가 dist 의 `.cur`/`.ani` 에서 32·64px 그림을 꺼내 `.cape` 로 굽는다(새로 그리지 않아 빌드 그대로). 칸 ↔ 맥 이름은 `win-cursor/mac.json` 하나를 `gen/cape.py` 와 같이 읽는다. 컨테이너에서 dist 몇 벌을 `build.cursor_bytes` 로 만들어 Playwright 로 눌러 봤다 — 기본 모양 전기·분홍은 `gen/cape.py` 와 픽셀·핫스팟·장 수·길이까지 같고, 폭죽은 반투명 칸 몇 개가 캔버스 반올림으로 2/255 다름. 둥근 모양(고르지 않은 30fps 틱)도 한 바퀴 1.07초 그대로, 색조 120° 도 맞게 돈다. 파일이 캔버스 PNG 라 `gen/cape.py` 보다 3배쯤 크다(전기 709KiB)
- 다음 할 것: Pages 에서 버튼이 `.cape` 를 내려주는지 보고, 맥에서 그 파일을 한 번 적용해 본다. 릴리스에 넣으려면 CHANGELOG 절을 먼저 쓴다
- 막힌 것: 없음. `docs/macos.md` 목록 4~8(24장 거절, 2x 선명도 등)은 아직

- 여기까지 됨 (2026-09-24 열두째 — 오른쪽 통 맞춤 · 사파리 끊김): 넓은 화면 오른쪽 통 카드를 줄여 1920×950 · 1440×900 · 1440×800 에서 넘침 0 (1280×720 은 29px, 1100×760 은 46px 넘쳐 제 스크롤로 넘어감). 애니메이션을 가볍게 했다 — 원래 크기·원래 색 그림은 캔버스(`toDataURL`)를 안 거치고 짧은 blob 주소로 끼우고, 스타일시트를 프레임마다 통째로 갈던 것을 바뀐 칸 style 만 바꾸게, 커서는 마우스가 올라간 칸 하나에만(규칙으로 박으면 .c-hand 283칸을 다 다시 따졌다), 썸네일은 화면에 보이는 것만, 크기·색조를 바꾸면 전 변환 그림을 버림, 마우스 없는 기기는 커서 그림을 안 만듦. 크롬 1440×900 · 전기 고르고 카드에 마우스 · 10초: toDataURL 125→0/s, 스타일 글 543→0KB/s, 메인 스레드 29→18%, 스타일 재계산 18→7%. main a65a1ffc
  이어서 아이폰 사파리가 열리게 됐고(사용자 확인), 남은 "프레임마다 깜빡임"은 그림을 먼저 decode 해 붙들어 둔 뒤 주소를 넘기게 해 고쳤다. 컨테이너에 `apt-get install webkit2gtk-driver gir1.2-webkit2-4.1 python3-gi-cairo` 로 WebKitGTK 2.50 을 깔고 390×844 로 띄워 10ms 마다 찍어 빈 칸 장을 셈 (`/usr/bin/python3.12`, `WEBKIT_DISABLE_SANDBOX_THIS_IS_DANGEROUS=1`, `xvfb-run`): 오늘 전 판은 썸네일 10–12% · 카드 6–12% 가 빈 장, 지금은 모양 2 × 구성표 3 × 600장, 카드 2 × 500장 모두 0
  색조 막대: 끄는 동안 정확한 색 그림은 한 번에 한 벌만 굽고(중간 값 건너뜀), 그 사이는 보이는 그림과의 각도 차만큼 CSS hue-rotate 를 카드·나머지 칸에 얹어 바로 반응하게 했다. 사용자가 본 "놓아야 적용"은 크롬(CPU 6배 느리게)·WebKitGTK 가짜 input 드래그로는 재현이 안 됐다 — 직전 판도 드래그 중 35번 바뀜. WebKit: 필터가 90단계 중 89단계에 바로 붙고, 놓은 뒤 106ms 에 정확한 그림, 드래그 시간은 전후 같음(4.0초)
- 다음 할 것: 아이폰·맥 사파리에서 깜빡임이 사라졌는지, 색조 막대가 끄는 동안 바로 따라오는지 사용자가 볼 것. 아이폰 크롬에서 아이콘이 안 뜨던 것도 다시 볼 것
- 막힌 것: 없음 — WebKit 은 apt 로 깔면 된다 (컨테이너가 바뀌면 다시 깔아야 함)

- 여기까지 됨 (2026-09-24 열한째 — 시안 페이지 좌우 분리): 1100px 이상 화면에서 시안 페이지를 좌우로 나눴다. 왼쪽 통(머리말·처음 한 번·구성표 고르기·내 커서 그리기·그 밖에)만 스크롤되고 오른쪽 통(여섯 칸·만져 보기)은 붙박이다. 페이지 자체는 스크롤하지 않고 두 통 높이는 아래 주소 줄 높이(`--bar`, JS 로 잼)만큼 비켜 앉는다. 좁은 화면은 `display:contents` + `order` 로 예전과 같은 순서로 한 줄. 폰(390px)에서 긴 설치 명령 줄 때문에 페이지가 671px 로 옆으로 밀리던 것(main 에도 있던 것)도 `minmax(0,1fr)` 로 막았다. `preview.tpl.html` 만 고쳐 커서 파일은 그대로다. Playwright 로 1440×900 · 1100×760 · 390×844 에서 잼
- 다음 할 것: Pages 에서 좌우 분리가 보이는지 확인. 오른쪽 통은 1440×900 에서도 622px 넘쳐(나머지 11칸 · 만져 보기) 따로 스크롤된다 — 한 화면에 다 넣고 싶으면 넓은 화면에서 카드를 줄이는 것이 다음 손잡이
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 열째 — 맥 조사): 57종을 macOS 에서 쓰는 길을 조사해 `docs/macos.md` 에 남겼다. 공식 길은 없고 비공개 API 를 쓰는 Mousecape(이어받은 판 Mousecape-swiftUI v1.2.0)가 사실상 유일하다. `win-cursor/gen/cape.py` 가 전기 구성표를 `.cape`(224KiB, 맥 이름 33개, 32장→24장)로 뽑고, **사용자 맥에서 적용이 됐다** — 화살표가 번쩍이며 돌고 I빔·손가락도 바뀌고 앱을 넘나들고 재부팅해도 남는다. Gatekeeper 는 "그래도 열기"로 넘었다. 빌드는 안 건드렸다
- 다음 할 것: 사용자가 맥에서 폭죽 `.cape`(144장→24장, 10fps)를 본다. 봐줄 만하면 시안 페이지에서 구성표마다 `.cape` 굽기(②-B, 설치 안내에 Gatekeeper 단계 포함), 아니면 여기서 접는다
- 막힌 것: 없음. 24장 제한이 실제로 걸리는지·빠른 FrameDuration·2x 선명도(`docs/macos.md` 목록 4~8)는 아직 안 봤다

- 여기까지 됨 (2026-09-24 아홉째 — 폭죽 슬로모션과 상세 카드): 폭죽만 60fps 로 한 바퀴를 1.5배 늘렸다(144장 × 1틱 · 2.4초 — 번쩍임·흰 십자·끝물 깜빡임·로켓 불똥은 실제 시간 그대로, `python3 win-cursor/gen/firework.py` — 기본값이 이 그림이고 바이트까지 같게 다시 나온다. K=3 에서 부동소수점 오차로 음수 거듭제곱(복소수)이 나 죽던 것을 고쳤다). 카멜레온·파문도 60fps(25505616)와 2배 슬로모션(71510ab5)까지 해 봤으나 사용자 결정으로 30fps(0fb7a108 그림)로 되돌렸다 — 카멜레온은 60fps 로도 장 사이 색 차이가 최대 9/255 라 거의 안 달라졌다. 파문 2배를 다시 뽑으려면 `RP_K=6 RP_RATE=1 RP_HOLD=1 RP_AA=1 python3 rip8.py`. 시안 페이지 썸네일이 꼬리를 자르던 것(5d7a33b1)과 상세 카드가 32칸 판 중 22칸 창만 보여 자르던 것(32칸 × 4px 로 넓힘)을 고쳤다 — 세 모양 × 342장 잘림 0
- 다음 할 것: Pages 에서 폭죽이 2.4초로 도는지와 상세 카드가 넓어졌는지 확인. 윈도우가 1틱 `.ani` 를 실제로 1/60초로 도는지 윈도우 PC 에서 볼 것
- 막힌 것: 폭죽 생성기는 `win-cursor/gen/firework.py` 로 저장소에 넣었다. 나머지(`cham8.py`·`rip8.py` 등)는 여전히 scratchpad 에만 있어 컨테이너가 회수되면 사라진다. 그림(`art/`)은 커밋돼 있으니 결과는 남지만 다시 뽑을 수는 없다

- 여기까지 됨 (2026-09-24 여덟째 — 남은 움직이는 여섯 종): claude/build-speed 를 이 세션 작업과 합쳐 main 에 올렸다(96ede315). 스캔(짙은 남색 몸을 흰 코어 줄이 훑고 청록 잔광, 32장×2 · 1.07초 — 쉬는 동안 테두리가 어두워 어두운 바탕에서 사라지던 것을 밝혀 고침), 폭죽(밤하늘 몸에서 로켓이 솟아 금·분홍·청록 세 발이 엇박으로 터짐, 불티는 STAR, 48장×2 · 1.6초), 눈 내림(흰 몸, 먼·중간·큰 눈 세 겹 + 비탈 눈 모자, 36장×4 · 2.4초 — 겨울밤 남색 몸 안은 `SN_MODE=night python3 snow8.py`), 카멜레온(몸 전체가 한 색으로 물들고 새 색이 끝에서 꼬리로 번지며 여섯 색을 4초에 한 바퀴, 30장×8 — 섞으면 120프레임), 파문(하늘빛 수면에 두 방울이 엇박으로 떨어져 동심원, 40장×3 · 2초 — 매끈한 모양에서 고리가 조금 각짐), 전구 간판(버건디 판 둘레를 전구가 한 칸씩 돌고 한 바퀴에 한 번 쇼 타임, 48장×2 · 1.6초 — 기호 칸 기호는 옛 금테 색 그대로). 생성기는 scratchpad 의 `scan8.py`·`fw8.py`·`snow8.py`·`cham8.py`·`rip8.py`·`marq8.py`. 미리보기: https://claude.ai/artifact/9k5cGRpVRuH4SacSrwTivp (Version 4). 여섯 종 모두 장 수가 늘어 이 구성표들의 빌드 일이 몇 배로 늘었다
- 다음 할 것: 사용자 검토 뒤 main 에 merge. 파문 고리가 각진 것과 전구 간판 기호 색이 눈에 걸리는지 미리보기로 볼 것. 전기·용암은 09-23 에 다뤄 이번에 뺐다
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24 일곱째 — 여섯째 묶음 다듬기): 글자비를 **매트릭스**로 이름을 바꿨다(id 는 coderain 그대로). 세 종에 디테일을 더했다 — 빗줄기(가까운 3칸 긴 줄기와 먼 1칸 방울로 원근, 왕관 물튀김·경사 따라 퍼지는 물결·웅덩이 반짝임, 36장 · 한 바퀴 1.2초 — 이 구성표 빌드 일이 1.5배, 되돌리려면 `RF_N=24 python3 rain7.py`), 매트릭스(세로줄을 앞·중간·뒤 세 겹으로 나눠 속도·꼬리 길이를 달리함, 40% 는 줄 중간에서 새로 생김, 꼬리 글자 7% 가 연초록으로 반짝임, 48장), 기포(바닥 구멍 서너 곳에서 6·8·12장 간격으로 올라오며 가속, 25% 는 흰 하이라이트 단 2×2 로 자람, 흰 점 → 사선 물튀김 → 수면 반짝임 3장으로 터짐, 모래시계 칸은 허리를 지나감). 세 종을 서브에이전트 셋이 동시에 만들어 약 5분 30초 · 에이전트당 토큰 147–156k. 생성기는 scratchpad 의 `rain7.py`·`mtx7.py`·`bub7.py`. 미리보기: https://claude.ai/artifact/Kk8FTvHuhTxxMALo7mAAny (Version 3). 커밋 8a8f03f1 · e36a6d72
- 다음 할 것: 사용자 검토 뒤 main 에 merge. 남은 움직이는 커서를 셋씩 병렬로 — 스캔, 폭죽, 눈 내림 / 카멜레온, 파문, 전구 간판. 기포가 화살표에서 8번·기다림에서 3번 한 장씩 비는 것이 움직임에서 눈에 걸리는지 미리보기로 볼 것
- 막힌 것: claude/build-speed 머지는 사용자가 직접 말해야 한다 — 다른 세션의 조언을 붙여넣은 것만으로는 자동 모드가 막았다

- 여기까지 됨 (2026-09-24 여섯째 묶음): 이발소 기둥(45° 사선 띠가 감겨 올라감), 글자비(흰 머리·초록 꼬리 줄기가 황금비 엇박으로 쏟아짐 48장), 빗줄기(세로줄마다 방울이 떨어져 바닥에서 튐), 기포(불투명한 물에 꽉 찬 방울이 흔들리며 올라와 수면에서 터짐, 테두리 짙은 남색 통일), 회전(빨·초·노·파 네 날 바람개비 24장·한 바퀴 1.2초 — 서브에이전트가 따로 만듦). 전부 art .txt 만 고쳤다(코드 무변경). 생성기는 scratchpad 의 `batch6.py`·`spin6.py`, 옛 그림은 `4a4e8bbd` 에서 읽는다. 미리보기: https://claude.ai/artifact/8yNovEEMQHPtGtkn2ayDi3 (Version 5). 커밋 a2067230 · bd41dd0c · 2ac6bfa2
- 다음 할 것: 사용자 검토 뒤 main 에 merge. 남은 움직이는 커서 — 스캔, 폭죽, 눈 내림, 카멜레온, 파문, 전구 간판(전기·용암은 이미 따로 다룸). 병렬 서브에이전트는 한 종 4분 40초·토큰 172k 였다
- 막힌 것: 없음

- 여기까지 됨 (2026-09-24): 움직이는 커서 다듬기 다섯째 묶음 — 불꽃(모닥불처럼 윗면에서 불길이 솟음, 알파 253 `RISE` + `smooth.rise_sheet` 가 새 윗변 위에 한 장으로 폄), 반짝이는 별(✦ 별 셋이 피었다 짐, 알파 252 `STAR` + `smooth.star`), 글리치 떨림(몸째 줄 단위로 찢기며 떨리고 둘레에 TV 눈, `smooth.jolts_of`·`_snow_of`·`_jolt` — 57종 중 글리치만 켜짐), 파도(노을 바다에 너울이 말려 부서짐). 두근두근은 예전 그림으로 되돌렸다. 그림 생성기는 scratchpad 의 `batch5b.py` 라 저장소에는 결과 art 만 있다. 미리보기: https://claude.ai/artifact/WyLUpdNuKDxF8EEr7ezsAb (Version 5). 전체 빌드 933초 (2026-09-24 리눅스 컨테이너 4코어, smooth.py 수정으로 57종 전부 다시 그림 · CPU 합 47분 — CLAUDE.md 의 197초는 코어가 더 많은 PC 값이라 바로 견주지 않는다)
- 다음 할 것: 사용자 검토 뒤 main 에 merge. 남은 움직이는 커서 13종을 다섯씩 — 전기, 이발소 기둥, 글자비, 빗줄기, 기포, 용암, 회전, 스캔, 폭죽, 눈 내림, 카멜레온, 파문, 전구 간판. `v1.4.0` 태그는 아래 옛 줄대로 로컬에서 민다
- 막힌 것: Pages 확인을 이 클라우드 세션에서 못 한다 — 네트워크 정책이 ruminem.github.io 를 막는다. 1b6c1f4 배포는 Actions 로그로 성공만 봤다

- 여기까지 됨: 구성표 57종(움직이는 20종, 갈래 5개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, 내 백업 20개(내보내기·가져오기)와 지금 설정의 적용 주소 줄(2026-09-19: 그 줄에 주소를 붙여넣거나 고쳐 Enter·적용으로 그 설정으로 바꿀 수도 있다 — 백업이 쓰는 cpUse 와 같은 검사·같은 길), cat-follower 고양이, GitHub Actions 검사, v1.3.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실(기하)에 테마 색을 자리대로 떠 넣는(색) 방식 — 몸도 외곽선도 자리대로 떠서 무늬·움직임·바깥 번짐에 더해 테두리를 타고 도는 효과까지 남고, 몸을 통째로 옮긴 색(글리치의 빨강청록 어긋남)은 새 몸을 그만큼 밀어 밑에 깐다. 몸 안쪽을 돔으로 보고 기울기로 명암·능선에 광택·오른쪽 아래에 접지 그림자를 넣어 볼륨감을 낸다(SHADOW 상수는 켜진 채로 배포 중). 크기(32·64·128)마다 새로 그리고, 떨어져 나온 조각(불꽃·눈송이·꽃잎)도 새 모양 둘레에 다시 흩는다. **2026-09-19: 음영을 높이장으로 바꾸고 재질을 붙였다** — 높이장을 BLUR(돔 반지름의 0.30배, 출력 픽셀이 아니라 비율이라 큰 판에서도 능선이 안 살아남는다)로 뭉갠 뒤 중앙차분으로 법선을 내고 램버트+블린퐁+림을 얹는다. 세기는 구성표가 고른 재질(MATERIALS: plastic·metal·glass·glow·cloth)이 정하고, schemes.json 의 "material" 에 89종을 각 구성표의 desc 를 보고 배정했다(2026-09-20 에 57종으로 줄인 뒤로는 39종에 붙어 있고 나머지 18종이 기본 plastic). 재질은 스텐실이 아니라 paint 쪽에 있다 — 스텐실은 테마와 무관해야 57종이 한 벌을 나눠 쓰기 때문. 무광(matte)은 뒀다 뺐다: 플라스틱에서 광택만 죽인 것이라 32px 에서 95분위 2.3–6.3 밖에 안 벌어져 눈에 안 걸렸다. test_light.py 가 접힘(뭉개기를 껐을 때 대비 0.35배 아래)과 재질(32px 에서 플라스틱과 8 이상 갈리는지, pink 와 chrome 둘로 잰다 — pink 만 재면 광택만 다른 재질이 묻힌다)을 본다. **불꽃 문턱(SPECK)을 2 → 5 로 올리고 불꽃을 둥근 점으로 찍는다** — 2 일 때 골드의 반짝이(5칸)가 몸에 합쳐져 옆구리에 혹이 났다. 5 는 분포에서 골랐다(떨어진 덩어리 2,472개 중 97%가 5칸 이하, 6칸 위로는 73개뿐이라 더 올려도 `sum(조각)*8 > 몸` 마개가 나머지를 잡는다). 영향은 gold 9칸·sakura 5칸·pencil 1칸으로 121종 중 3종뿐이었고, 그중 sakura·pencil 은 57종으로 줄일 때 빠져서 지금 남은 것은 gold 하나다(smooth.py 의 SPECK 주석에 적힌 "3종"은 121종 때 센 값 그대로다 — 주석 한 글자만 고쳐도 빌드 표식이 깨져 전부 다시 그리므로 안 건드렸다). **세는 코드를 짤 때 원본 그림을 그대로 보면 틀린다** — build.smooth_parts 가 테마 기호(?·모래시계)를 먼저 떼어 내고 화살표 몸을 물려주므로 specks_of 가 보는 것은 기호가 빠진 프레임이고, 칸 목록도 ROLES 가 아니라 ROLES+EXTRA 다. **2026-09-19: 테마 색을 칸이 아니라 영역으로 뜬다** — 이웃과의 대비가 KEEP(115) 아래면 한 영역으로 묶고(크롬의 계조는 한 덩어리, 쿠키의 초코칩·민트초코의 체크는 갈림), 소속도가 가장 큰 영역 안에서만 섞어서 색 경계가 칸 모서리 대신 곡선으로 난다. 색 뜨는 자리의 격자(UV)도 31 → 127 로 올려 그 곡선이 다시 계단지지 않게 했다. 큰 판에서 무늬가 네모로 보이던 것이 이걸로 없어졌다(크롬은 이어진 금속, 오션의 하이라이트는 동그란 점, 표범 무늬는 유기적인 얼룩). test_light.py 가 PATTERN 0/1 의 색 가짓수로 이게 실제로 도는지 본다(2026-09-20 실측 크롬 4.29배·민트초코 1.81배·분홍 1.00배 — 121종이던 때는 6.6배·3.0배였다). 시안 페이지 제목 오른쪽에 `v1.3.0 · 커밋7자` 가 박힌다 — Pages 에 방금 민 커밋이 실렸는지 새로고침 한 번으로 가르려는 것(build.py 의 version(), test_stamp.py 가 형식을 본다). 빌드 결과(dist·data·preview.html)는 커밋하지 않고 CI 가 만들어 Pages(Actions 원본)에 올린다. 그림이 어떻게 나오는지는 빌드 없이 `python sheet.py <모양들> <구성표들>` 로 3초 만에 한 장으로 본다(`--frames` 는 움직임, `--set KEEP=40` 처럼 상수를 그 판에서만 바꿔 견줄 수 있다). **2026-09-19: CI 가 `dist`·`data`·`.build-stamp.json` 을 캐시한다** — 히트면 잡 51~60초(빌드 8초), 미스면 121종일 때 12분·57종으로 줄인 뒤 10분(2026-09-20 실측, `.stencils.pkl` 도 같이 캐시한다). 캐시는 브랜치마다 저장되고 다른 브랜치가 받아 쓸 수 있는 것은 main 이 저장한 것뿐이라, 브랜치를 새로 딴 직후 한 번은 12분이 걸린다(고장이 아니다). 재면서 **커서 바이트가 러너 OS 를 탄다는 것도 확인했다** — 윈도우 파이썬은 zlib-ng 1.3.1, 리눅스는 zlib 1.3 이라 12,947개가 전부 갈리고, 압축을 풀어 픽셀만 견주면 1,093개(8.4%)가 남는다. 그 1,093개는 매끈한 모양의 wait·ibeam 뿐이고 채널값 1,548,288개 중 15개(가장 큰 차이 50)라 libm(acos·tan 등)이 1ulp 갈리는 것으로 보인다 — sqrt 는 IEEE 로 정해져 있어 안 갈린다. **그래서 빌드를 ubuntu 러너로 옮기는 안은 접었다**: 올라가는 커서가 러너를 타게 된다. **2026-09-20: 빌드가 왜 오래 걸리는지 재고 둘을 고쳤다** — 121종 224–268초 가운데 순수 규모가 146초(65%), 워커마다 스텐실을 다시 굽는 것이 18초(430번 중 270번이 중복), 일감 30개를 워커 12개가 나누다 남는 꼬리가 51초였다. 규모는 구성표 네 갈래 64종(손그림·미니멀, 색과 무늬, 자연·계절, 빛과 화면)을 빼서 57종으로 줄였고(175–181초), 스텐실 160벌은 build.py 가 먼저 병렬로 구워 `.stencils.pkl` 에 모아 두고 워커가 읽어 쓰며 일감은 모양당 4묶음·오래 걸리는 data 먼저로 바꿨다(147–160초, dist 의 sha256 은 그대로). 워커를 잰 방법: PYTHONPATH 에 sitecustomize.py 를 두고 atexit 로 워커마다 len(smooth._cache) 와 process_time 을 pid 별 파일에 남겼다 — 프로젝트 소스를 안 건드리고 잰다(한 파일에 같이 붙이면 찢긴다). 워커 12개를 같이 돌리면 혼자일 때의 5.7배 밖에 안 나온다(물리 코어 6). **2026-09-20: 움직이는 커서를 30fps 로 올렸다** — `smooth.FPS = 30` 이 켜져 있고, 다 그린 뒤 프레임 사이를 알파 곱한 값으로 섞어 끼운다. 핵심은 **한 바퀴 도는 시간을 지키는 것**이다: 340개 중 136개가 rate 5·7 이라 반으로 자르면 20% 빨라지므로, `steps()` 가 합이 보존되게 쪼개고(4→[2,2] · 5→[3,2] · 6→[2,2,2] · 7→[3,2,2] · 8→[2,2,2,2]) 고르지 않은 것은 `.ani` 의 프레임별 `rate` 칸에 적는다(`curs_to_ani` 가 목록을 받으면 그 칸을 넣고, 못 읽는 프로그램을 위해 anih 에는 첫 조각을 적어 둔다). 만든 dist .ani 2,140개를 전부 파싱해 한 바퀴 시간이 그림과 같은지 확인했고(어긋난 것 0개, 그중 1,620개가 rate 칸을 쓴다), 윈도우 LoadCursorFromFile 로도 읽힌다. 조각 수는 **내림**으로 잡는다 — 올림하면 rate 7 이 4조각(34fps)이 되어 파일만 커진다. **기본 모양과 KEEP 칸은 안 섞는다**: 테마 픽셀아트를 섞으면 중간색이 끼어 뿌옇다. 그 길을 올리려면 그림에 프레임을 그려 넣어야 해서 글리치·빗줄기 두 종은 실제로 그렸다 — 글리치는 가로줄을 따라 찢고(`interp_art.py --mode tear`, 섞으면 몸이 통째로 움직이는 물건이라 잔상이 낀다), 빗줄기는 물방울을 한 줄씩 내린다(`--mode drops`). 빗줄기는 여섯 장 중 셋이 복사본(A B C A B C)이던 자리를 채운 것이라 **용량이 안 늘고** 30fps 가 됐다. 값: 전체 빌드 155초 → 197초, dist 123MB → 247MB. **2026-09-22: 전기의 불꽃을 가닥으로, 용암을 12프레임으로 바꿨다** — 전기는 몸에서 떨어진 1칸 점 서넛이 전부라 "점이 찍힌다"는 말이 맞았다. 4~5칸짜리 지그재그 두세 줄로 바꾸되 `smooth.specks_of` 의 두 마개를 지켜야 매끈한 모양이 그걸 불꽃으로 본다 — **조각 하나가 SPECK(5칸)을 넘거나 조각 총합×8 이 몸 칸수를 넘으면 인식을 통째로 포기**해 가닥이 몸에 먹힌다(arrow 예산 13칸, hand 7칸). 가닥은 **한 번에 한 축만** 움직인다: 대각으로 달리면 칸끼리 모서리로만 닿아 픽셀에서도 점선으로 읽힌다. 뿌리는 몸에 대각으로만 닿는 자리에서 시작한다 — 한 칸 더 띄우면 허공에 뜬 점이 되고, 4방향으로 닿으면 몸에 합쳐진다. `smooth.specks()` 는 이제 조각별로 묶어 받아 **같은 조각 안에서 이웃이던 점 사이를 채운다**(이웃 판정은 그 조각에서 가장 가까운 두 점 사이 거리). 몸이 LIMIT 20칸으로 펴지면서 원본에서 붙어 있던 칸이 점 지름(cells/LIMIT)보다 멀어지는 것이 점선의 원인이었다. 덤으로 골드의 5칸 반짝이가 원본 그림대로 십자(`+`)로 나온다 — 전에는 점 다섯 개로 흩어졌다. `test_light.py` 가 이걸 본다(판·몸 크기를 실제 빌드 비율로 잡아야 한다 — 작게 잡으면 점끼리 저절로 겹쳐 헛검사가 된다). 용암은 6 → 12프레임이고 `rate 5` 를 그대로 둬서 한 바퀴가 0.5초 → 1초다. 안쪽 무늬는 세로로 순환하는 값 노이즈(가로 5.5칸·세로 3칸 격자, 주기 12칸이라 프레임당 1칸씩 내려감)로 다시 칠했고 외곽선과 실루엣은 원본 그대로다. **프레임마다 그 장의 값만 분위수로 잘라 색 비율을 고정한다** — 프레임 전부를 통째로 자르면 어떤 장은 전부 밝고 어떤 장은 전부 어두워 커서가 깜빡인다. 그림을 만든 스크립트는 scratchpad 에 뒀다(`art/` 가 원본이라 저장소에 두면 진실이 둘이 된다). **2026-09-22: CI 캐시 크기를 실측했다** — zstd 압축 후 243,661,752 바이트(232 MiB), 압축 전은 dist 256M + data 48M + `.stencils.pkl` 15M = 319MB 다(`Cache saved with key` 줄에 그대로 찍힌다). 저장·복원이 6초밖에 안 걸리는 것은 러너–Azure 구간이 180 MB/s 라서지 캐시가 안 먹어서가 아니다. GitHub 한도는 **레포당 10GB·7일 미사용 evict·넘치면 오래된 것부터**라 232MB 짜리는 43개가 한계고, 세션 브랜치마다 한 벌씩 저장되면 main 캐시까지 밀려날 수 있다. **캐시를 저장소에 커밋하는 안은 접었다** — 커밋마다 232MB 가 `.git` 에 영구히 쌓인다(이미 227MB 고 그래서 dist 를 뺀 저장소다). 그리고 **코드를 고쳤을 때의 9분은 캐시 탓이 아니다**: `_hashes()` 가 build.py·make_cur.py·shape.py·smooth.py·shapes.json 을 묶은 해시를 구성표 57종 **전부의** 표식에 넣으므로, 캐시가 100% 맞아떨어져도 전부 다시 그린다. 캐시가 실제로 먹는다는 증거는 run #77(문서만 고친 커밋, main)이 전체 1분 52초에 끝난 것이다. **세션 브랜치도 main 캐시를 제대로 받아 온다** — 문서만 고친 커밋을 세션 브랜치에 밀어 잰 run #81 의 빌드 스텝이 **2초**(캐시 복원 8초, 잡 전체 41초)였다. 그래서 main 에 주기적으로 캐시를 굽는 스케줄 잡은 **안 붙였다**: 브랜치라서 미스가 나는 게 아니고, 남는 위험은 7일 넘게 main 을 안 만져 evict 되는 경우뿐인데 그게 실제로 일어난 증거가 아직 없다. 브랜치 첫 run 이 다시 10분 걸리는 것을 보면 그때 붙이면 된다(주 1회 40초짜리 잡 하나. 캐시는 읽기만 해도 7일 시계가 다시 돈다). **2026-09-23: 전기를 흐르는 전류로, 용암을 떨어지는 방울로 다시 만들고, 기호 칸 회귀를 고쳤다** — 그림은 둘 다 `a885635a` 원본에서 새로 뽑았다(scratchpad 의 `fx.py`, 저장소에는 결과 art 만 둔다). 전기는 32프레임 · rate 2 로, 원본에서 번쩍임을 걷어낸 바탕(칸마다 가장 흔한 어두운 몸/노란 선) 위에 펄스 둘이 `smooth.around` 차례로 테두리를 돈다 — 매끈한 모양도 외곽선 색을 같은 차례로 떠서 그대로 따라 돈다. 가닥은 펄스 머리 근처 앵커에서만 튀고 모양은 2프레임마다 바뀐다. rate 3 으로 먼저 했다가 매끈한 모양이 20fps 로 남는 것을 보고 바꿨다(`steps(3, 30)` 은 안 쪼갠다. 섞어 끼우면 도는 펄스가 잔상이 된다). 용암은 24프레임 · rate 4 로, 가장 낮은 끝(꼬리)과 4칸 넘게 떨어진 다음 끝(날개)에서 방울이 맺혀 떨어진다(`DRIP` 한 살이 12프레임, 날개는 24프레임에 한 번 엇박). 떨어지는 방울은 위 1칸 · 아래 2칸 — 1칸 폭은 매끈한 모양에서 점 하나로, 2×2 는 네모 딱지로 읽혔다. **smooth.py 세 군데**: 불꽃 비율을 모든 프레임 공통 몸(core) 네모로 잰다(방울이 끝에 붙어 몸이 길어지는 프레임에 다른 방울이 튀지 않게. 57종 중 jitter · firework · snowfall 이 따라 바뀌는데 눈으로는 구분이 안 됐다), **몸에 대각으로 닿은 3칸 이상 조각(STRAND)은 새 몸 테두리로 당긴다**(전기 가닥이 매끈한 몸 옆에 뜬 대시로 보였다. 다른 구성표의 닿은 조각은 전부 1~2칸이라 안 걸린다), 조각이 원본 한 칸의 u·v 크기를 들고 다녀 **축마다 따로** 이웃을 가른다(11×17 이 65×79 로 펴지면 가로 한 칸이 세로보다 커서, 가장 가까운 두 점 거리로 가르면 가로 이웃이 빠져 점선이 됐다). **기호 칸 회귀**: 도움말 · 작업 · 위치 · 사용자는 매끈한 모양에서 화살표와의 차이를 기호로 떼는데, 9-22 작업이 무늬와 불꽃을 칸마다 따로 뽑아 화살표 부분이 섞여 들어 매끈한 몸 위에 네모 픽셀이 박혀 있었다(시안 페이지에 실린 v1.4.0 에 있음). 새 화살표 프레임에 원본 기호를 얹어 만들고, `test_light.py` 가 57종 전부에서 기호 상자 밖 3칸 넘게 샌 칸이 없는지 본다(1칸 여유로 짰다가 스스로 움직이는 firework · jitter 기호가 걸렸다. 9-22 그림에서는 8칸 전부 잡힌다). 미리보기: https://claude.ai/artifact/TQQRWY116ncUjTHtdqtoYP (개선 전후를 커서 파일과 같은 프레임 시간으로 재생). **2026-09-23 두 번째: 전기 가닥을 번개로, 용암 방울을 매끈한 꼬리 끝에 매달았다** — 전기 가닥은 몸에서 수직으로 뻗는 막대라 털처럼 보였고(사용자 평: 짜침) 매끈한 모양에서는 4px 막대였다. 생성기에서 펄스 진행 반대쪽으로 뒤·밖을 번갈아 딛게 하고 머리마다 60% 확률로만 튀긴다. 매끈한 모양은 뻗은 가닥(STRAND)을 뿌리→끝 사선의 마디마다 좌우로 꺾는 가는 꺾은선 + 푸른 번짐으로 그린다(`BOLT_*` 상수) — 칸끼리 그대로 이으면 ㄴ·ㄱ 회로선, 가운데만 밀면 물결이 됐다. 용암은 사용자가 '커서 밖에서 떨어진다'고 했다: 방울이 몸에 붙어 있는 동안은 몸의 일부라 매끈한 모양에서 안 보였고 떨어진 뒤에야 비율 자리로 나타났다. 방울 칸을 알파 254(`smooth.HANG`)로 칠해 따로 떼고, 매달린 몸 칸이 새 테두리에 오게 옮긴다. 바로 붙어 있던 조각은 그 칸까지 이어 그린다. 다른 55종은 매끈한 모양 330장을 옛 코드와 해시로 견줘 한 픽셀도 안 바뀌었다. **60fps 시험 빌드**(`smooth.FPS = 60` 만 바꾼 복사본, 같은 컨테이너에서 차례로): 매끈한 모양 프레임과 커서 파일이 정확히 2배(전기 둥근 화살표 197→403KB, 용암 369→758KB), dist 274→559MB, data 는 그대로 59MB, 빌드 435→533초(한 번씩만 잼 — 같은 30fps 가 332초로 끝난 적도 있다). 기본 모양은 그림 프레임 그대로라 무관하다. 비교는 미리보기 아티팩트 2판의 '30 · 60fps' 절

- 다음 할 것: **`v1.4.0` 태그를 밀 것** — 60fps 는 사용자가 미리보기로 보고 눈으로 차이가 작다고 해 30 으로 정했다(바꾸면 dist 가 2배인 559MB). 태그 절차: CHANGELOG 1.4.0 절은 다 썼고(높이장 음영·재질·불꽃 문턱·57종·30fps·전기 가닥·용암 12프레임) 태그만 밀면 release.yml 이 그 절을 본문으로 써서 zip 까지 만든다. **태그는 클라우드 세션에서 못 민다** — 2026-09-22 에 `git push origin refs/tags/v1.4.0` 이 HTTP 403 으로 막혔다(브랜치 푸시는 된다. 세션 자격증명이 태그 ref 를 못 만든다). 로컬에서 `git pull && git tag -a v1.4.0 -m v1.4.0 && git push origin v1.4.0` 하면 release.yml 이 받는다. release 잡은 2026-09-24 부터 main 캐시를 받기만 한다(`actions/cache/restore`) — 첫 태그 로그에서 `Cache restored` 가 찍히는지 볼 것(안 찍히면 전처럼 처음부터 13분 안팎). **dist 가 247MB 로 두 배가 된 것을 지켜볼 것** — Pages 에 통째로 올라가므로 업로드가 길어지면 `smooth.FPS` 를 24 나 20 으로 낮추는 것이 첫 손잡이다(상수 하나). 기본 모양과 KEEP 칸(크기조정·손·펜·십자·위)은 아직 8~15fps 다 — 올리려면 글리치·빗줄기처럼 그림에 프레임을 그려 넣어야 하고, 중복 프레임이 있는 구성표부터가 공짜다(`dup_frames.py` 기준 179칸). 빌드 꼬리 — 마지막 모양(bevel)의 4묶음이 137초에 시작해 160초에 끝나 그 사이 워커 8개가 논다. data 일감(모양당 하나, 121종 때 혼자 29초)도 쪼개면 더 줄 수 있으나 shape_data 가 sids 와 무관하게 전부 그리는 구조라 먼저 그걸 갈라야 한다. 영역 경계를 더 둥글리기 — 지금은 둘러싼 2×2 의 소속도로 갈라서 금색·아가일에서 경계가 살짝 각진다. 3×3 커널이면 둥글어지지만 자리마다 보는 칸이 4배다. 매끈한 모양 10가지 중 쓸 것만 남기기(모양 하나에 약 9MB) — `python sheet.py all pink,ink,chrome,electric,glitch` 한 장 보고 고르면 되고, 그때 매끈한 몸이 테마 그림보다 15% 큰 것(LIMIT 20칸 대 17칸)이 의도인지도 같이 정하기. 전기의 '테두리를 도는 전류'가 매끈한 모양에서는 프레임마다 테두리 전체가 한 색으로 쏠리는 것. 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)

- 막힌 것: 없음. 전체 빌드는 사용자 PC(5600X, 워커 12) 262.4초 @ d33cecab(2026-09-24, 그 전 커밋 191.5초 — 전기·용암·무지개 프레임을 늘려 일이 41% 늘었다), 클라우드 컨테이너(워커 4) 323–330초 · 커서 6,099개. **2026-09-24 빌드 속도 작업(claude/build-speed)**: 일감을 (모양 × 구성표) 하나씩 프레임 수로 어림한 큰 것부터 넣고(모양당 4덩이 때 무거운 구성표가 한 덩이에 몰려 꼬리가 PC 에서 22초 어림), 스텐실은 코드 해시(`"*stencil"`)가 바뀔 때만 굽고, 코드 해시에 파이썬·zlib 버전을 넣었다(PNG 바이트가 버전을 탐 — 받는 날 한 번 전부 다시 그림). 전후 커서·data·preview 6,110개가 바이트까지 같다(d33cecab 기준, 컨테이너). 컨테이너 전체 330.5→323.2초, 무지개 흐름 하나 40.6→29.9초, 아무것도 안 고침 0.1초. PC 에서 새로 재 보는 것이 남았다. 시간의 86%가 `smooth.paint` 였다(cProfile) — 뜬 색으로 칸 색을 돌려 쓰게 해서 컨테이너 `--all` 415.8→283.5초 @ b0b19190(바이트 동일). 남은 큰 덩이는 data 일감(모양당 20초 남짓)이다. build.py·smooth.py·make_cur.py·shape.py·shapes.json 중 하나라도 고치면 표식이 통째로 깨져 전부 다시 그린다. 이미 쌓인 .git 227MB 는 그대로 — 되돌리려면 history rewrite 라 안 건드렸다. **맥에서 이어갈 때**: `git pull` 하고 파이썬 3.14 로 `cd win-cursor && python build.py`(표준 라이브러리만 쓴다). 첫 빌드는 캐시가 없어 전부 다시 그리고, 만든 `preview.html` 은 브라우저로 그냥 열면 된다. 커서 파일을 윈도우가 읽는지 보는 점검(LoadCursorFromFile)과 handler.ps1·install.ps1 계열은 윈도우 전용이라 맥에서는 못 돌린다 — 그건 CI 가 본다
