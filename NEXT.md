- 여기까지 됨: 구성표 121종(움직이는 20종, 갈래 9개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, 내 백업 20개(내보내기·가져오기)와 지금 설정의 적용 주소 줄(2026-09-19: 그 줄에 주소를 붙여넣거나 고쳐 Enter·적용으로 그 설정으로 바꿀 수도 있다 — 백업이 쓰는 cpUse 와 같은 검사·같은 길), cat-follower 고양이, GitHub Actions 검사, v1.3.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실(기하)에 테마 색을 자리대로 떠 넣는(색) 방식 — 몸도 외곽선도 자리대로 떠서 무늬·움직임·바깥 번짐에 더해 테두리를 타고 도는 효과까지 남고, 몸을 통째로 옮긴 색(글리치의 빨강청록 어긋남)은 새 몸을 그만큼 밀어 밑에 깐다. 몸 안쪽을 돔으로 보고 기울기로 명암·능선에 광택·오른쪽 아래에 접지 그림자를 넣어 볼륨감을 낸다(SHADOW 상수는 켜진 채로 배포 중). 크기(32·64·128)마다 새로 그리고, 떨어져 나온 조각(불꽃·눈송이·꽃잎)도 새 모양 둘레에 다시 흩는다. **2026-09-19: 음영을 높이장으로 바꾸고 재질을 붙였다** — 높이장을 BLUR(돔 반지름의 0.30배, 출력 픽셀이 아니라 비율이라 큰 판에서도 능선이 안 살아남는다)로 뭉갠 뒤 중앙차분으로 법선을 내고 램버트+블린퐁+림을 얹는다. 세기는 구성표가 고른 재질(MATERIALS: plastic·metal·glass·glow·cloth)이 정하고, schemes.json 의 "material" 에 89종을 각 구성표의 desc 를 보고 배정했다(나머지 32종은 기본 plastic). 재질은 스텐실이 아니라 paint 쪽에 있다 — 스텐실은 테마와 무관해야 121종이 한 벌을 나눠 쓰기 때문. 무광(matte)은 뒀다 뺐다: 플라스틱에서 광택만 죽인 것이라 32px 에서 95분위 2.3–6.3 밖에 안 벌어져 눈에 안 걸렸다. test_light.py 가 접힘(뭉개기를 껐을 때 대비 0.35배 아래)과 재질(32px 에서 플라스틱과 8 이상 갈리는지, pink 와 chrome 둘로 잰다 — pink 만 재면 광택만 다른 재질이 묻힌다)을 본다. **불꽃 문턱(SPECK)을 2 → 5 로 올리고 불꽃을 둥근 점으로 찍는다** — 2 일 때 골드의 반짝이(5칸)가 몸에 합쳐져 옆구리에 혹이 났다. 5 는 분포에서 골랐다(떨어진 덩어리 2,472개 중 97%가 5칸 이하, 6칸 위로는 73개뿐이라 더 올려도 `sum(조각)*8 > 몸` 마개가 나머지를 잡는다). 영향은 gold 9칸·sakura 5칸·pencil 1칸으로 121종 중 3종뿐. **세는 코드를 짤 때 원본 그림을 그대로 보면 틀린다** — build.smooth_parts 가 테마 기호(?·모래시계)를 먼저 떼어 내고 화살표 몸을 물려주므로 specks_of 가 보는 것은 기호가 빠진 프레임이고, 칸 목록도 ROLES 가 아니라 ROLES+EXTRA 다. **2026-09-19: 테마 색을 칸이 아니라 영역으로 뜬다** — 이웃과의 대비가 KEEP(115) 아래면 한 영역으로 묶고(크롬의 계조는 한 덩어리, 쿠키의 초코칩·민트초코의 체크는 갈림), 소속도가 가장 큰 영역 안에서만 섞어서 색 경계가 칸 모서리 대신 곡선으로 난다. 색 뜨는 자리의 격자(UV)도 31 → 127 로 올려 그 곡선이 다시 계단지지 않게 했다. 큰 판에서 무늬가 네모로 보이던 것이 이걸로 없어졌다(크롬은 이어진 금속, 오션의 하이라이트는 동그란 점, 표범 무늬는 유기적인 얼룩). test_light.py 가 PATTERN 0/1 의 색 가짓수로 이게 실제로 도는지 본다(크롬 6.6배·민트초코 3.0배·분홍 1.00배). 시안 페이지 제목 오른쪽에 `v1.3.0 · 커밋7자` 가 박힌다 — Pages 에 방금 민 커밋이 실렸는지 새로고침 한 번으로 가르려는 것(build.py 의 version(), test_stamp.py 가 형식을 본다). 빌드 결과(dist·data·preview.html)는 커밋하지 않고 CI 가 만들어 Pages(Actions 원본)에 올린다. 그림이 어떻게 나오는지는 빌드 없이 `python sheet.py <모양들> <구성표들>` 로 3초 만에 한 장으로 본다(`--frames` 는 움직임, `--set KEEP=40` 처럼 상수를 그 판에서만 바꿔 견줄 수 있다). **2026-09-19: CI 가 `dist`·`data`·`.build-stamp.json` 을 캐시한다** — 히트면 잡 51~60초(빌드 8초), 미스면 12분. 캐시는 브랜치마다 저장되고 다른 브랜치가 받아 쓸 수 있는 것은 main 이 저장한 것뿐이라, 브랜치를 새로 딴 직후 한 번은 12분이 걸린다(고장이 아니다). 재면서 **커서 바이트가 러너 OS 를 탄다는 것도 확인했다** — 윈도우 파이썬은 zlib-ng 1.3.1, 리눅스는 zlib 1.3 이라 12,947개가 전부 갈리고, 압축을 풀어 픽셀만 견주면 1,093개(8.4%)가 남는다. 그 1,093개는 매끈한 모양의 wait·ibeam 뿐이고 채널값 1,548,288개 중 15개(가장 큰 차이 50)라 libm(acos·tan 등)이 1ulp 갈리는 것으로 보인다 — sqrt 는 IEEE 로 정해져 있어 안 갈린다. **그래서 빌드를 ubuntu 러너로 옮기는 안은 접었다**: 올라가는 커서가 러너를 타게 된다

- 다음 할 것: 높이장 음영·재질·불꽃 문턱은 v1.3.0 태그 뒤에 들어간 것이라 아직 어느 릴리스에도 없다 — CHANGELOG 에 1.4.0 절을 쓰고 태그를 밀 것(태그를 밀면 release.yml 이 그 절을 본문으로 써서 zip 까지 만든다). 측정용 임시 워크플로 `.github/workflows/perf.yml` 은 다 재고 나면 지우기(`perf/**` 푸시에서만 돌아 main 에서는 안 돈다). 영역 경계를 더 둥글리기 — 지금은 둘러싼 2×2 의 소속도로 갈라서 금색·아가일에서 경계가 살짝 각진다. 3×3 커널이면 둥글어지지만 자리마다 보는 칸이 4배다. 매끈한 모양 10가지 중 쓸 것만 남기기(모양 하나에 약 9MB) — `python sheet.py all pink,ink,chrome,electric,glitch` 한 장 보고 고르면 되고, 그때 매끈한 몸이 테마 그림보다 15% 큰 것(LIMIT 20칸 대 17칸)이 의도인지도 같이 정하기. 전기의 '테두리를 도는 전류'가 매끈한 모양에서는 프레임마다 테두리 전체가 한 색으로 쏠리는 것. 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)

- 막힌 것: 없음. 전체 빌드 263초(2026-09-19 측정, 높이장 음영·재질이 붙기 전에는 209초) · 커서 12,947개 · dist 150MB + data 52MB. build.py·smooth.py·make_cur.py·shape.py·shapes.json 중 하나라도 고치면 표식이 통째로 깨져 전부 다시 그린다(보수적이라 그대로 뒀다). 아무것도 안 고쳤으면 1초, 구성표 하나면 17초. 이미 쌓인 .git 227MB 는 그대로 — 되돌리려면 history rewrite 라 안 건드렸다. **맥에서 이어갈 때**: `git pull` 하고 파이썬 3.14 로 `cd win-cursor && python build.py`(표준 라이브러리만 쓴다). 첫 빌드는 캐시가 없어 전부 다시 그리고, 만든 `preview.html` 은 브라우저로 그냥 열면 된다. 커서 파일을 윈도우가 읽는지 보는 점검(LoadCursorFromFile)과 handler.ps1·install.ps1 계열은 윈도우 전용이라 맥에서는 못 돌린다 — 그건 CI 가 본다
