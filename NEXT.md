- 여기까지 됨: 구성표 57종(움직이는 20종, 갈래 5개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, 내 백업 20개(내보내기·가져오기)와 지금 설정의 적용 주소 줄(2026-09-19: 그 줄에 주소를 붙여넣거나 고쳐 Enter·적용으로 그 설정으로 바꿀 수도 있다 — 백업이 쓰는 cpUse 와 같은 검사·같은 길), cat-follower 고양이, GitHub Actions 검사, v1.3.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실(기하)에 테마 색을 자리대로 떠 넣는(색) 방식 — 몸도 외곽선도 자리대로 떠서 무늬·움직임·바깥 번짐에 더해 테두리를 타고 도는 효과까지 남고, 몸을 통째로 옮긴 색(글리치의 빨강청록 어긋남)은 새 몸을 그만큼 밀어 밑에 깐다. 몸 안쪽을 돔으로 보고 기울기로 명암·능선에 광택·오른쪽 아래에 접지 그림자를 넣어 볼륨감을 낸다(SHADOW 상수는 켜진 채로 배포 중). 크기(32·64·128)마다 새로 그리고, 떨어져 나온 조각(불꽃·눈송이·꽃잎)도 새 모양 둘레에 다시 흩는다. **2026-09-19: 음영을 높이장으로 바꾸고 재질을 붙였다** — 높이장을 BLUR(돔 반지름의 0.30배, 출력 픽셀이 아니라 비율이라 큰 판에서도 능선이 안 살아남는다)로 뭉갠 뒤 중앙차분으로 법선을 내고 램버트+블린퐁+림을 얹는다. 세기는 구성표가 고른 재질(MATERIALS: plastic·metal·glass·glow·cloth)이 정하고, schemes.json 의 "material" 에 89종을 각 구성표의 desc 를 보고 배정했다(2026-09-20 에 57종으로 줄인 뒤로는 39종에 붙어 있고 나머지 18종이 기본 plastic). 재질은 스텐실이 아니라 paint 쪽에 있다 — 스텐실은 테마와 무관해야 57종이 한 벌을 나눠 쓰기 때문. 무광(matte)은 뒀다 뺐다: 플라스틱에서 광택만 죽인 것이라 32px 에서 95분위 2.3–6.3 밖에 안 벌어져 눈에 안 걸렸다. test_light.py 가 접힘(뭉개기를 껐을 때 대비 0.35배 아래)과 재질(32px 에서 플라스틱과 8 이상 갈리는지, pink 와 chrome 둘로 잰다 — pink 만 재면 광택만 다른 재질이 묻힌다)을 본다. **불꽃 문턱(SPECK)을 2 → 5 로 올리고 불꽃을 둥근 점으로 찍는다** — 2 일 때 골드의 반짝이(5칸)가 몸에 합쳐져 옆구리에 혹이 났다. 5 는 분포에서 골랐다(떨어진 덩어리 2,472개 중 97%가 5칸 이하, 6칸 위로는 73개뿐이라 더 올려도 `sum(조각)*8 > 몸` 마개가 나머지를 잡는다). 영향은 gold 9칸·sakura 5칸·pencil 1칸으로 121종 중 3종뿐이었고, 그중 sakura·pencil 은 57종으로 줄일 때 빠져서 지금 남은 것은 gold 하나다(smooth.py 의 SPECK 주석에 적힌 "3종"은 121종 때 센 값 그대로다 — 주석 한 글자만 고쳐도 빌드 표식이 깨져 전부 다시 그리므로 안 건드렸다). **세는 코드를 짤 때 원본 그림을 그대로 보면 틀린다** — build.smooth_parts 가 테마 기호(?·모래시계)를 먼저 떼어 내고 화살표 몸을 물려주므로 specks_of 가 보는 것은 기호가 빠진 프레임이고, 칸 목록도 ROLES 가 아니라 ROLES+EXTRA 다. **2026-09-19: 테마 색을 칸이 아니라 영역으로 뜬다** — 이웃과의 대비가 KEEP(115) 아래면 한 영역으로 묶고(크롬의 계조는 한 덩어리, 쿠키의 초코칩·민트초코의 체크는 갈림), 소속도가 가장 큰 영역 안에서만 섞어서 색 경계가 칸 모서리 대신 곡선으로 난다. 색 뜨는 자리의 격자(UV)도 31 → 127 로 올려 그 곡선이 다시 계단지지 않게 했다. 큰 판에서 무늬가 네모로 보이던 것이 이걸로 없어졌다(크롬은 이어진 금속, 오션의 하이라이트는 동그란 점, 표범 무늬는 유기적인 얼룩). test_light.py 가 PATTERN 0/1 의 색 가짓수로 이게 실제로 도는지 본다(2026-09-20 실측 크롬 4.29배·민트초코 1.81배·분홍 1.00배 — 121종이던 때는 6.6배·3.0배였다). 시안 페이지 제목 오른쪽에 `v1.3.0 · 커밋7자` 가 박힌다 — Pages 에 방금 민 커밋이 실렸는지 새로고침 한 번으로 가르려는 것(build.py 의 version(), test_stamp.py 가 형식을 본다). 빌드 결과(dist·data·preview.html)는 커밋하지 않고 CI 가 만들어 Pages(Actions 원본)에 올린다. 그림이 어떻게 나오는지는 빌드 없이 `python sheet.py <모양들> <구성표들>` 로 3초 만에 한 장으로 본다(`--frames` 는 움직임, `--set KEEP=40` 처럼 상수를 그 판에서만 바꿔 견줄 수 있다). **2026-09-19: CI 가 `dist`·`data`·`.build-stamp.json` 을 캐시한다** — 히트면 잡 51~60초(빌드 8초), 미스면 121종일 때 12분·57종으로 줄인 뒤 10분(2026-09-20 실측, `.stencils.pkl` 도 같이 캐시한다). 캐시는 브랜치마다 저장되고 다른 브랜치가 받아 쓸 수 있는 것은 main 이 저장한 것뿐이라, 브랜치를 새로 딴 직후 한 번은 12분이 걸린다(고장이 아니다). 재면서 **커서 바이트가 러너 OS 를 탄다는 것도 확인했다** — 윈도우 파이썬은 zlib-ng 1.3.1, 리눅스는 zlib 1.3 이라 12,947개가 전부 갈리고, 압축을 풀어 픽셀만 견주면 1,093개(8.4%)가 남는다. 그 1,093개는 매끈한 모양의 wait·ibeam 뿐이고 채널값 1,548,288개 중 15개(가장 큰 차이 50)라 libm(acos·tan 등)이 1ulp 갈리는 것으로 보인다 — sqrt 는 IEEE 로 정해져 있어 안 갈린다. **그래서 빌드를 ubuntu 러너로 옮기는 안은 접었다**: 올라가는 커서가 러너를 타게 된다. **2026-09-20: 빌드가 왜 오래 걸리는지 재고 둘을 고쳤다** — 121종 224–268초 가운데 순수 규모가 146초(65%), 워커마다 스텐실을 다시 굽는 것이 18초(430번 중 270번이 중복), 일감 30개를 워커 12개가 나누다 남는 꼬리가 51초였다. 규모는 구성표 네 갈래 64종(손그림·미니멀, 색과 무늬, 자연·계절, 빛과 화면)을 빼서 57종으로 줄였고(175–181초), 스텐실 160벌은 build.py 가 먼저 병렬로 구워 `.stencils.pkl` 에 모아 두고 워커가 읽어 쓰며 일감은 모양당 4묶음·오래 걸리는 data 먼저로 바꿨다(147–160초, dist 의 sha256 은 그대로). 워커를 잰 방법: PYTHONPATH 에 sitecustomize.py 를 두고 atexit 로 워커마다 len(smooth._cache) 와 process_time 을 pid 별 파일에 남겼다 — 프로젝트 소스를 안 건드리고 잰다(한 파일에 같이 붙이면 찢긴다). 워커 12개를 같이 돌리면 혼자일 때의 5.7배 밖에 안 나온다(물리 코어 6). **2026-09-20: 움직이는 커서를 30fps 로 올렸다** — `smooth.FPS = 30` 이 켜져 있고, 다 그린 뒤 프레임 사이를 알파 곱한 값으로 섞어 끼운다. 핵심은 **한 바퀴 도는 시간을 지키는 것**이다: 340개 중 136개가 rate 5·7 이라 반으로 자르면 20% 빨라지므로, `steps()` 가 합이 보존되게 쪼개고(4→[2,2] · 5→[3,2] · 6→[2,2,2] · 7→[3,2,2] · 8→[2,2,2,2]) 고르지 않은 것은 `.ani` 의 프레임별 `rate` 칸에 적는다(`curs_to_ani` 가 목록을 받으면 그 칸을 넣고, 못 읽는 프로그램을 위해 anih 에는 첫 조각을 적어 둔다). 만든 dist .ani 2,140개를 전부 파싱해 한 바퀴 시간이 그림과 같은지 확인했고(어긋난 것 0개, 그중 1,620개가 rate 칸을 쓴다), 윈도우 LoadCursorFromFile 로도 읽힌다. 조각 수는 **내림**으로 잡는다 — 올림하면 rate 7 이 4조각(34fps)이 되어 파일만 커진다. **기본 모양과 KEEP 칸은 안 섞는다**: 테마 픽셀아트를 섞으면 중간색이 끼어 뿌옇다. 그 길을 올리려면 그림에 프레임을 그려 넣어야 해서 글리치·빗줄기 두 종은 실제로 그렸다 — 글리치는 가로줄을 따라 찢고(`interp_art.py --mode tear`, 섞으면 몸이 통째로 움직이는 물건이라 잔상이 낀다), 빗줄기는 물방울을 한 줄씩 내린다(`--mode drops`). 빗줄기는 여섯 장 중 셋이 복사본(A B C A B C)이던 자리를 채운 것이라 **용량이 안 늘고** 30fps 가 됐다. 값: 전체 빌드 155초 → 197초, dist 123MB → 247MB. **2026-09-22: 전기의 불꽃을 가닥으로, 용암을 12프레임으로 바꿨다** — 전기는 몸에서 떨어진 1칸 점 서넛이 전부라 "점이 찍힌다"는 말이 맞았다. 4~5칸짜리 지그재그 두세 줄로 바꾸되 `smooth.specks_of` 의 두 마개를 지켜야 매끈한 모양이 그걸 불꽃으로 본다 — **조각 하나가 SPECK(5칸)을 넘거나 조각 총합×8 이 몸 칸수를 넘으면 인식을 통째로 포기**해 가닥이 몸에 먹힌다(arrow 예산 13칸, hand 7칸). 가닥은 **한 번에 한 축만** 움직인다: 대각으로 달리면 칸끼리 모서리로만 닿아 픽셀에서도 점선으로 읽힌다. 뿌리는 몸에 대각으로만 닿는 자리에서 시작한다 — 한 칸 더 띄우면 허공에 뜬 점이 되고, 4방향으로 닿으면 몸에 합쳐진다. `smooth.specks()` 는 이제 조각별로 묶어 받아 **같은 조각 안에서 이웃이던 점 사이를 채운다**(이웃 판정은 그 조각에서 가장 가까운 두 점 사이 거리). 몸이 LIMIT 20칸으로 펴지면서 원본에서 붙어 있던 칸이 점 지름(cells/LIMIT)보다 멀어지는 것이 점선의 원인이었다. 덤으로 골드의 5칸 반짝이가 원본 그림대로 십자(`+`)로 나온다 — 전에는 점 다섯 개로 흩어졌다. `test_light.py` 가 이걸 본다(판·몸 크기를 실제 빌드 비율로 잡아야 한다 — 작게 잡으면 점끼리 저절로 겹쳐 헛검사가 된다). 용암은 6 → 12프레임이고 `rate 5` 를 그대로 둬서 한 바퀴가 0.5초 → 1초다. 안쪽 무늬는 세로로 순환하는 값 노이즈(가로 5.5칸·세로 3칸 격자, 주기 12칸이라 프레임당 1칸씩 내려감)로 다시 칠했고 외곽선과 실루엣은 원본 그대로다. **프레임마다 그 장의 값만 분위수로 잘라 색 비율을 고정한다** — 프레임 전부를 통째로 자르면 어떤 장은 전부 밝고 어떤 장은 전부 어두워 커서가 깜빡인다. 그림을 만든 스크립트는 scratchpad 에 뒀다(`art/` 가 원본이라 저장소에 두면 진실이 둘이 된다)

- 다음 할 것: **`v1.4.0` 태그를 밀 것** — CHANGELOG 1.4.0 절은 다 썼고(높이장 음영·재질·불꽃 문턱·57종·30fps) 태그만 밀면 release.yml 이 그 절을 본문으로 써서 zip 까지 만든다. **dist 가 247MB 로 두 배가 된 것을 지켜볼 것** — Pages 에 통째로 올라가므로 업로드가 길어지면 `smooth.FPS` 를 24 나 20 으로 낮추는 것이 첫 손잡이다(상수 하나). 기본 모양과 KEEP 칸(크기조정·손·펜·십자·위)은 아직 8~15fps 다 — 올리려면 글리치·빗줄기처럼 그림에 프레임을 그려 넣어야 하고, 중복 프레임이 있는 구성표부터가 공짜다(`dup_frames.py` 기준 179칸). 빌드 꼬리 — 마지막 모양(bevel)의 4묶음이 137초에 시작해 160초에 끝나 그 사이 워커 8개가 논다. data 일감(모양당 하나, 121종 때 혼자 29초)도 쪼개면 더 줄 수 있으나 shape_data 가 sids 와 무관하게 전부 그리는 구조라 먼저 그걸 갈라야 한다. 영역 경계를 더 둥글리기 — 지금은 둘러싼 2×2 의 소속도로 갈라서 금색·아가일에서 경계가 살짝 각진다. 3×3 커널이면 둥글어지지만 자리마다 보는 칸이 4배다. 매끈한 모양 10가지 중 쓸 것만 남기기(모양 하나에 약 9MB) — `python sheet.py all pink,ink,chrome,electric,glitch` 한 장 보고 고르면 되고, 그때 매끈한 몸이 테마 그림보다 15% 큰 것(LIMIT 20칸 대 17칸)이 의도인지도 같이 정하기. 전기의 '테두리를 도는 전류'가 매끈한 모양에서는 프레임마다 테두리 전체가 한 색으로 쏠리는 것. 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)

- 막힌 것: 없음. 전체 빌드 197초(2026-09-20 측정, 57종 · 30fps; `FPS` 를 켜기 전에는 147–161초, 121종이던 2026-09-19 에는 224–268초) · 커서 6,099개 · dist 247MB + data 47MB. build.py·smooth.py·make_cur.py·shape.py·shapes.json 중 하나라도 고치면 표식이 통째로 깨져 전부 다시 그린다(보수적이라 그대로 뒀다). 아무것도 안 고쳤으면 1.5초(preview.html 은 늘 다시 만든다), 구성표 하나면 12초. 이미 쌓인 .git 227MB 는 그대로 — 되돌리려면 history rewrite 라 안 건드렸다. **맥에서 이어갈 때**: `git pull` 하고 파이썬 3.14 로 `cd win-cursor && python build.py`(표준 라이브러리만 쓴다). 첫 빌드는 캐시가 없어 전부 다시 그리고, 만든 `preview.html` 은 브라우저로 그냥 열면 된다. 커서 파일을 윈도우가 읽는지 보는 점검(LoadCursorFromFile)과 handler.ps1·install.ps1 계열은 윈도우 전용이라 맥에서는 못 돌린다 — 그건 CI 가 본다
