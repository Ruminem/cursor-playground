- 여기까지 됨: 구성표 121종(움직이는 20종, 갈래 9개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, cat-follower 고양이, GitHub Actions 검사, v1.0.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실에 테마 색을 자리대로 떠 넣는 방식 — 무늬·움직임·바깥 번짐이 그대로 남고, 커서 파일에 담는 크기(32·64·128)마다 새로 그린다
- 다음 할 것: 매끈한 모양 10가지 중 실제로 쓸 만한 것을 골라 안 쓸 것은 빼기(모양 하나에 약 9MB). 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)
- 막힌 것: 없음. 빌드는 88초(프로세스 11개로 나눠 돌림), dist 101MB + data 36MB. 큰 판에서는 테마 무늬가 네모지게 보이는데(픽셀 그림을 자리대로 떠 오기 때문) 기본 크기에서는 안 보여서 그냥 뒀음
