- 여기까지 됨: 구성표 121종(움직이는 20종, 갈래 9개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, 시안 페이지에서 내 백업 20개(내보내기·가져오기)와 지금 설정의 적용 주소 줄, cat-follower 고양이, GitHub Actions 검사, v1.2.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실에 테마 색을 자리대로 떠 넣는 방식 — 몸도 외곽선도 자리대로 떠서 무늬·움직임·바깥 번짐에 더해 테두리를 타고 도는 효과(전기의 전류)까지 남고, 몸을 통째로 옮긴 색(글리치의 빨강청록 어긋남)은 색이 아니라 옮김이 효과라 새 몸을 그만큼 밀어 밑에 깐다. 매끈한 모양은 이제 몸 안쪽을 돔으로 보고 그 기울기로 명암을, 능선에 좁은 광택을, 오른쪽 아래에 접지 그림자를 넣어 볼륨감을 낸다 — 명암은 몸 색을 곱해서 내므로 2색 테마(분홍·잉크)에서도 먹힌다. 커서 파일에 담는 크기(32·64·128)마다 새로 그린다. 몸에서 떨어져 나온 조각(전기 불꽃·눈송이·꽃잎)도 새 모양 둘레에 다시 흩는다. 처리 스크립트는 모르는 주소를 받으면 스스로 새 파일로 갈아 끼운다. 빌드 결과(dist·data·preview.html)는 커밋하지 않고 CI 가 만들어 Pages(Actions 원본)에 올린다 — 커밋마다 26~74MB 씩 저장소가 영구히 불던 것을 막았다. 태그를 밀면 release.yml 이 zip 과 릴리스까지 낸다. 그림이 어떻게 나오는지는 빌드 없이 `python sheet.py <모양들> <구성표들>` 로 3초 만에 한 장으로 본다(밝은·어두운 바탕 나란히, `--frames` 는 움직임) — 그려서 재 보는 임시 스크립트를 세션마다 39번 다시 짜던 것을 도구로 남겼다
- 다음 할 것: **접지 그림자를 남길지 정하기** — 음영만 넣으면 dist +16.3%·빌드 +26%, 그림자까지 넣으면 +31.3%·+57%(옛 88초 → 138초). 눈으로 보고 값을 조이거나(SHADE·SPEC·LIFT·SINK·DOME 이 smooth.py 머리에 상수로 있음) 그림자를 모양별로만 켜는 것도 길이다. 전기의 '테두리를 도는 전류'를 매끈한 모양에서 살리기 — 글리치는 몸을 통째로 옮긴 것이라 유령 층으로 풀었지만 전기는 외곽선 위 밝은 점이 자리를 옮기는 다른 기전이라 아직 남아 있다(지금은 프레임마다 테두리 전체가 한 색 쪽으로 쏠린다). 매끈한 모양 10가지 중 실제로 쓸 만한 것을 골라 안 쓸 것은 빼기(모양 하나에 약 9MB) — `python sheet.py all pink,ink,chrome,electric,glitch` 한 장을 보고 고르면 된다. 그때 매끈한 모양의 몸이 테마 그림보다 15% 큰 것(LIMIT 20칸 대 17칸)이 의도인지도 같이 정하기. 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)
- 막힌 것: 없음. 전체 빌드는 138초(돔 음영 넣기 전 88초), dist 135MB + data 47MB. shapes.json 을 글자 한 자만 고쳐도 표식이 통째로 깨져 12,947개를 다시 그린다 — 코드·설정 해시를 구성표마다 묶어 두었기 때문이고, 보수적이라 그대로 뒀다. .build-stamp.json 덕에 두 번째부터는 고친 것만 다시 그린다(무변경 1초, 구성표 하나 17초). 이미 쌓인 .git 227MB 는 그대로 남아 있음 — 되돌리려면 history rewrite 라 안 건드렸다. 큰 판에서는 테마 무늬가 네모지게 보이는데(픽셀 그림을 자리대로 떠 오기 때문) 기본 크기에서는 안 보여서 그냥 뒀음
