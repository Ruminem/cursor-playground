- 여기까지 됨: 구성표 121종(움직이는 20종, 갈래 9개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, cat-follower 고양이, GitHub Actions 검사, v1.0.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실에 테마 색을 자리대로 떠 넣는 방식 — 몸도 외곽선도 자리대로 떠서 무늬·움직임·바깥 번짐에 더해 테두리를 타고 도는 효과(전기의 전류, 글리치의 빨강청록 어긋남)까지 남고, 커서 파일에 담는 크기(32·64·128)마다 새로 그린다. 몸에서 떨어져 나온 조각(전기 불꽃·눈송이·꽃잎)도 새 모양 둘레에 다시 흩는다. 처리 스크립트는 모르는 주소를 받으면 스스로 새 파일로 갈아 끼운다. 빌드 결과(dist·data·preview.html)는 커밋하지 않고 CI 가 만들어 Pages(Actions 원본)에 올린다 — 커밋마다 26~74MB 씩 저장소가 영구히 불던 것을 막았다. 태그를 밀면 release.yml 이 zip 과 릴리스까지 낸다
- 다음 할 것: 매끈한 모양 10가지 중 실제로 쓸 만한 것을 골라 안 쓸 것은 빼기(모양 하나에 약 9MB). 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)
- 막힌 것: 없음. 전체 빌드는 100~200초, dist 101MB + data 36MB. .build-stamp.json 덕에 두 번째부터는 고친 것만 다시 그린다(무변경 1초, 구성표 하나 17초). 이미 쌓인 .git 227MB 는 그대로 남아 있음 — 되돌리려면 history rewrite 라 안 건드렸다. 큰 판에서는 테마 무늬가 네모지게 보이는데(픽셀 그림을 자리대로 떠 오기 때문) 기본 크기에서는 안 보여서 그냥 뒀음
