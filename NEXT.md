- 여기까지 됨: 구성표 121종(움직이는 20종, 갈래 9개) × 커서 모양 11가지(기본 + 매끈한 10가지)·17칸·색조·크기, 시안 페이지에서 모양 탭·찾기·즐겨찾기·랜덤·그리기·시간대별 자동 전환, cat-follower 고양이, GitHub Actions 검사, v1.0.0 릴리스(zip). 매끈한 모양은 smooth.py 가 거리함수로 그린 스텐실에 테마 색을 끼워 넣는 방식(프레임마다 색을 다시 뽑아 움직임을 살림)
- 다음 할 것: 매끈한 모양 10가지 중 실제로 쓸 만한 것을 골라 안 쓸 것은 빼기(저장소가 줄어듦). 고양이가 커서를 따라 걷는 모습과 커서 크기 적용을 눈으로 확인. 그린 커서를 바로 적용하는 길(지금은 .cur 내려받아 수동 지정)
- 막힌 것: dist 가 127MB(모양 11가지). 모양 하나가 약 11MB 이고 움직이는 구성표의 .ani 가 한 개 40KB 라 모양을 늘리면 계속 커짐 — 모양을 더할 때마다 한 번 생각할 것
