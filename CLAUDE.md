# cursor-playground

윈도우 커서 구성표를 만들어 시안 페이지에서 바로 적용하게 하는 취미 프로젝트. 그리고 곁다리로
커서를 따라다니는 고양이. 파이썬은 표준 라이브러리만 쓴다 (의존성 없음).

## 어디에 뭐가 있나

### win-cursor — 본체
| 파일 | 하는 일 |
|---|---|
| `schemes.json` | 구성표 57종 목록. `build.py`·`install.ps1`·`handler.ps1` 이 다 같은 파일을 읽는다 |
| `shapes.json` | 커서 모양 목록. **첫 번째가 기본**(테마 그림 그대로), 나머지는 `smooth.py` 가 그린다 |
| `art/<구성표>/<칸>.txt` | 픽셀 그림 원본. 글자 한 자 = 한 칸, 머리에 `hotspot`/`color`/`rate` 줄. 알파가 `fe`(254)인 색은 **몸에 매달린 조각**(용암 방울)이라 매끈한 모양이 따로 떼어 새 꼬리 끝에 매단다(`smooth.HANG`) — 눈으로는 불투명과 같으니 다른 그림에 쓰면 조각이 뜯겨 나간다. 같은 식으로 `fd`(253)는 **몸 위로 솟는 조각**(모닥불 불길, `RISE`), `fc`(252)는 **몸 위 별**(`STAR`, 네 갈래 반짝이로 다시 그림)이다. 알파 표식이 아니라 그림 모양으로 켜지는 길도 하나 있다 — 장마다 몸의 가로줄이 통째로 옆으로 밀려 있으면(글리치) `smooth.jolts_of` 가 줄마다 밀림을 재서 새 몸 픽셀 줄을 같이 밀고, 무채색 반투명 칸은 TV 눈으로 떼어 네모로 찍는다. 57종 중 글리치만 켜진다(`test_light.py`) |
| `build.py` | 전부를 다시 만드는 입구. `python build.py` 하나로 `preview.html`·`win-cursor/dist/`·`win-cursor/data/`·README 표까지 |
| `smooth.py` | 거리함수로 매끈한 모양을 그리고(`stencil`) 테마 색을 자리대로 떠 넣는다(`paint`) |
| `make_cur.py` | `.txt` → PNG → `.cur`/`.ani` 변환만 담당. 그림을 해석하는 곳 |
| `shape.py` | `.txt` 읽고 쓰기, 글리프 자리 옮기기 |
| `sheet.py` | 모양 × 구성표(또는 프레임)를 PNG 한 장에 그려 눈으로 견주는 도구. 빌드 없이 3초. 빌드 해시에 안 들어가서 고쳐도 다시 그리지 않는다 |
| `gen/firework.py` | 폭죽 그림(`art/firework/`)을 다시 뽑는 생성기. 손으로 돌리고 빌드는 안 부른다. 빌드 코드 해시와 CI 캐시 키(`win-cursor/*.py`)에 안 들어가서 고쳐도 다시 그리지 않는다 — 돌려서 art 가 바뀌어야 반영된다 |
| `mac.json` | 윈도우 칸 → 맥 커서 이름. 시안 페이지의 **맥용 .cape 받기** 버튼과 `gen/cape.py` 가 같이 읽는다. 버튼은 새로 그리지 않고 dist 의 `.cur`/`.ani` 를 받아 브라우저에서 `.cape` 로 다시 담는다 (`docs/macos.md`) |
| `gen/cape.py` | 구성표 하나를 `.cape` 로 뽑는 손 도구(기본 모양만). 버튼의 기준 답 — 기본 모양이면 둘이 같은 그림·핫스팟·장 수가 나와야 한다 |
| `preview.tpl.html` | 시안 페이지 틀. `build.py` 가 여기에 데이터를 끼워 `preview.html` 을 뱉는다 |
| `handler.ps1` | `cursor-playground://` 주소를 받아 실제로 커서를 적용/되돌림. 받는 주소 목록은 파일 머리에 |
| `install.ps1` / `setup.ps1` | 주소 연결 등록과 설치 |

### cat-follower
`cat.py` (tkinter, `Cat` 클래스가 전부) + `art/*.txt` 그림. 본체와 아무 관계 없다.

## 고칠 때 순서
- **그림이 어떻게 나오는지는 빌드 전에 `python sheet.py <모양들> <구성표들>` 로 본다** (`--frames` 는 움직임,
  `all` 은 전부). 나온 PNG 를 Read 로 연다. 그려서 재 보는 임시 스크립트를 새로 짜지 않는다 — 모자라면
  `sheet.py` 에 옵션을 더한다. 2026-09-19 에 세어 보니 그런 스크립트를 39번 다시 짰고 출력 5만 자가 거기로 갔다
- 그림·구성표·모양을 고쳤으면 **`win-cursor` 에서 `python build.py` 를 돌린다.**
  `dist/`·`data/`·`preview.html` 은 **커밋하지 않는다**(`.gitignore`) — 커밋마다 저장소가 수십 MB 씩
  영구히 불어서 뺐다. CI 가 만들어 Pages 에 바로 올린다. `build.py` 가 같이 고치는 `win-cursor/README.md` 표는 커밋한다
- CI(`.github/workflows/check.yml`)가 빌드해서 커서 파일을 윈도우가 읽는지 보고,
  main 이면 그 결과를 GitHub Pages 에 올린다. **Pages 원본은 브랜치가 아니라 GitHub Actions** 여야 한다
- **이 저장소에서 "올렸다 · 반영했다 · 릴리스했다"는 Pages 에 실제로 실린 것을 본 때다.**
  커밋도 푸시도 CI 성공도 아니다 — 시안 페이지가 사람이 쓰는 물건이라 거기 안 실렸으면 아무것도 안 바뀐 것이다.
  확인은 https://ruminem.github.io/cursor-playground/win-cursor/preview.html 를 받아 이번에 바뀐 내용이
  들어 있는지 본다. **루트 URL 이 404 인 것은 정상이다** — 워크플로가 저장소 루트를 통째로 올려서 페이지가
  `/win-cursor/` 아래에 산다. 2026-09-19 이걸 모르고 배포가 깨졌다고 보고했다
- **시안 페이지 제목 오른쪽에는 버전과 커밋 7자가 늘 박혀 있어야 한다** — `v1.2.0 · f0cca36` 꼴.
  `build.py` 의 `version()` 이 `CHANGELOG.md` 맨 위 절과 `git rev-parse` 에서 뽑아 `preview.tpl.html` 의
  `<!--VERSION-->` 자리에 끼운다. 위의 Pages 확인이 여기에 기댄다 — 이게 있어야 지금 보고 있는 페이지가
  방금 민 커밋으로 만든 것인지 새로고침 한 번으로 갈린다. 버전 표시는 페이지 해시에도 들어가서
  그림이 그대로여도 커밋이 바뀌면 `preview.html` 을 다시 만든다. 형식이 깨지면 `test_stamp.py` 가 실패한다
- 릴리스는 태그(`v*`)를 밀면 `.github/workflows/release.yml` 이 빌드·zip·릴리스까지 한다.
  본문은 `CHANGELOG.md` 의 그 버전 절을 그대로 쓰므로 **CHANGELOG 를 먼저 쓰고 태그를 민다**
- 전체 빌드(`--all`)는 **사용자 PC(Ryzen 5 5600X, 워커 12개) 262.4초 @ d33cecab, 2026-09-24** — 커서 6,099개
  (57종 × 107칸 = 기본 17 + 매끈한 모양 10가지 × 9, 각각 3크기), 코어 수만큼 프로세스. 같은 PC 에서 그보다 앞선
  커밋은 191.5초였다. 클라우드 컨테이너(워커 4개)는 323–330초로 다른 기계라 PC 숫자와 견주지 않는다.
  시간의 86%가 `smooth.paint`(색 떠 넣기)였고 PNG 굽기(zlib)는 2.5% — 2026-09-24 컨테이너 cProfile.
  그래서 paint 가 **뜬 색으로** 칸 색을 돌려 쓰고(`_SAME`, 프레임·크기를 넘어), 섞은 색은 fill 이 같은 프레임끼리
  나눠 쓴다(`_BLENDS`) — 전기 × 둥근 모양 한 일감 39.8→13.6초, 컨테이너 `--all` 415.8→283.5초 @ b0b19190,
  6,110개 바이트 동일. **여기 계산식을 고칠 때 부동소수점 순서를 바꾸지 않는다** (`over` 주석 참고)
  **그림에 프레임을 더 그리면 빌드가 그만큼 길어진다** — 전기 32장·용암 24장·무지개 흐름 24장으로 늘린 뒤
  일이 41% 늘어 191.5→262.4초가 됐다. 움직이는 구성표 몇이 일의 대부분이다.
  일감은 (모양 × 구성표) 하나씩, 프레임 수로 어림한 큰 것부터 넣는다 — 모양 하나를 4덩이로 나누던 때는 무거운
  구성표가 한 덩이에 몰려 끝에 워커들이 놀았다(PC 262초 중 22초 어림). **시안 데이터(`data/<모양>/`)도 같은 일감이
  만든다** — 시안 그림(20·40칸)이 커서 32·64 판과 똑같은 그림이라 `smooth.drawer` 하나로 나눠 그리고, 조각은 부모가
  모양별로 모아 쓴다. 모양마다 data 일감을 따로 돌리던 때보다 컨테이너 `--all` 346.6→280.4초, 워커 시간 합 22분 25초→17분 38초
  (2026-09-24, 같은 커밋 위에서 전후, 6,110개 바이트 동일). (`smooth.FPS = 30` 을 켜기 전에는
  2026-09-20 PC 147–161초였고, 121종이던 때는 224–268초였다). 산출물은 `dist` 247MB + `data` 47MB (2026-09-24 컨테이너 `du`: 시안 데이터를 프레임 한 장씩에서 칸마다 세로 스트립 한 장으로 바꾼 뒤 data 218→98MB, preview.html 6.0→2.0MB) — **`FPS` 를 끄면 dist 가 121MB 로
  돌아간다.** 용량이 문제되면 여기가 첫 손잡이다. 2026-09-23 전기(32프레임)·용암(24프레임)을 늘린 뒤
  리눅스 `du` 로 dist 256→276MB · data 48→56MB (재는 곳이 달라 위 247MB 와 바로 견주지 않는다)
  매끈한 모양의 스텐실 160벌은 테마와 무관해서 `win-cursor/.stencils.pkl` 에 먼저 구워 워커들이 나눠 쓴다.
  스텐실은 코드 해시(표식의 `"*stencil"`)에만 기대서 그림만 고쳤으면 다시 굽지 않는다.
  재료가 그대로인 구성표는 `win-cursor/.build-stamp.json` 을 보고 건너뛰므로 두 번째부터는
  고친 것만 다시 그린다 (컨테이너에서 아무것도 안 고쳤으면 0.1초 — `preview.html` 은 늘 다시 만든다,
  무거운 구성표 하나(무지개 흐름)면 30초, 2026-09-24). 전부 다시 그리려면 `python build.py --all`.
  코드 해시에는 파이썬·zlib 버전도 들어간다 — PNG 바이트가 버전마다 달라서, 버전이 바뀌면 한 번 전부 다시 그린다
- CI 도 `dist`·`data`·`.build-stamp.json`·`.stencils.pkl` 을 캐시해 같은 수를 쓴다 — 히트면 잡 51초(빌드 8초),
  미스면 14분(빌드 스텝 12분 21초, 잡의 97%) — 2026-09-23 run #90 실측 (09-20 에는 10분이었고 그 뒤 프레임이 늘었다).
  **코드를 고치면 CI 는 전체 빌드를 두 번 한다** — 세션 브랜치에서 한 번(브랜치 캐시는 main 이 못 받는다),
  main 에 merge 해서 또 한 번. 그러니 코드 수정은 모아서 한 번에 main 에 넣는다. **캐시는 브랜치마다 따로 저장되고**, 다른 브랜치가 받아 쓸 수 있는 것은
  main 이 저장한 것뿐이다. 캐시 키는 `win-cursor` 의 `art/**`·`*.py`·`*.json`·`preview.tpl.html` 이라 문서만 고치면 히트한다.
  **세션 브랜치도 main 캐시를 제대로 받는다** — 2026-09-22 실측: 문서만 고친 커밋을 새 세션 브랜치에 밀었더니
  빌드 스텝 2초·잡 41초였다. 그러니 브랜치에서 10분이 나오면 브랜치 탓이 아니라 `*.py`·`*.json` 을 고쳐서다.
  **코드 파일 하나만 고쳐도 57종을 전부 다시 그린다** — `build.py` 의 `_hashes()` 가 build.py·make_cur.py·shape.py·smooth.py·shapes.json
  을 묶은 해시를 구성표 전부의 표식에 넣기 때문이고, 캐시가 100% 맞아떨어져도 그렇다. 캐시 한 벌은 zstd 압축 후
  232 MiB(243,661,752 바이트)이고 GitHub 한도는 레포당 10GB·7일 미사용 evict 다.
  `release.yml` 도 이 캐시를 받기만 한다(`actions/cache/restore`, 저장 안 함). 캐시 버전에 경로 목록이 들어가서
  **`path` 줄이 `check.yml` 과 글자 하나까지 같아야** 받아진다 — 그래서 받은 뒤 체크아웃 자리로 옮기는 스텝이 있다.
  태그 빌드가 캐시를 실제로 받는지는 다음 릴리스 로그의 `Cache restored` 로 확인해야 한다 (아직 안 봤다)
- 움직이는 커서의 fps 는 `smooth.py` 의 `FPS` 하나가 정한다 (지금 30). **60 은 2026-09-23 시험하고 접었다** — 그린 프레임 사이를 섞어 끼우는 것뿐이라 눈으로 차이가 작은데, 매끈한 모양 커서 파일과 dist 가 정확히 2배(274→559MB)였다. 더 부드럽게 하려면 fps 가 아니라 그림에 프레임을 더 그린다. 다 그린 뒤 프레임 사이를 섞어
  끼우는 것이라 그리는 값은 안 건드린다. **한 바퀴 도는 시간을 지키는 것이 이 기능의 전부다** —
  `steps()` 가 한 프레임의 rate 틱을 합이 보존되게 쪼개고(5 → `[3,2]`), 고르지 않게 쪼갠 것은
  `.ani` 의 프레임별 `rate` 칸에 적는다. 이게 깨지면 부드러워진 게 아니라 빨리 재생된 것이다 —
  `test_light.py` 가 합을 검사한다. 기본 모양과 KEEP 칸은 테마 그림을 그대로 쓰는 길이라 안 섞는다
  (픽셀아트를 섞으면 중간색이 끼어 뿌옇다). 그 길로 fps 를 올리려면 그림에 프레임을 직접 그려 넣는다
- 파이썬 버전은 3.14 로 맞춘다. PNG 압축(zlib) 결과가 버전마다 달라 올라가는 커서가 러너 환경을 타지 않게

## 읽지 말 것 (생성물)
`win-cursor/preview.html`(2.0MB), `win-cursor/dist/`, `win-cursor/data/`, `win-cursor/out/`, `win-cursor/.build-stamp.json`, `win-cursor/.stencils.pkl`, `win-cursor/README.md` 의 구성표 표.
전부 `build.py` 가 만든다. 궁금한 게 있으면 원본(`win-cursor/art/`, `*.json`, `win-cursor/preview.tpl.html`)을 본다.

## 이 저장소의 관례
- 주석·커밋 메시지·문서는 한국어. 소스 머리에 `// SPDX-License-Identifier: Apache-2.0`
- `handler.ps1` 은 한글 때문에 **UTF-8 BOM** 으로 저장해야 한다 (PowerShell 5.1 이 BOM 없으면 ANSI 로 읽음)
- 워크플로에서 한글을 찍는 스텝은 `shell: pwsh`, 파이썬은 `PYTHONUTF8: '1'`
- 세션을 마치면 `NEXT.md` 세 줄(여기까지 됨 / 다음 할 것 / 막힌 것)을 고친다
